# 研修シート追いツール — セットアップ手順

## 1. スプレッドシートを作成する

1. [Google スプレッドシート](https://sheets.google.com) を開く
2. 「空白のスプレッドシート」で新規作成
3. タイトルを「研修進捗管理」などに変更

---

## 2. GASコードを貼り付ける

1. メニューから **「拡張機能」→「Apps Script」** を開く
2. エディタに表示されているコードを全て削除
3. `training_tracker.gs` の内容を全てコピーして貼り付ける
4. 左上の💾（保存）ボタンをクリック（またはCmd+S / Ctrl+S）

---

## 3. 初期セットアップを実行する

1. スプレッドシートに戻る（タブを切り替え）
2. メニューに **「研修管理」** が表示される（表示されない場合はページを再読み込み）
3. **「研修管理」→「初期セットアップ実行」** をクリック
4. 初回実行時は権限の許可を求めるダイアログが表示されます
   - 「権限を確認」→「詳細」→「安全ではないページに移動」→「許可」
5. セットアップ完了のメッセージが表示されたらOK

> セットアップ完了後、シートにサンプルデータが追加されます。

---

## 4. 自動実行トリガーを設定する（重要）

1. **「研修管理」→「自動実行トリガー設定」** をクリック
2. 「毎朝8時に自動実行します」のメッセージが表示されたらOK

> これにより、毎朝8時に停滞チェックが自動で実行されます。

---

## 5. 日常の使い方

### メンバーを追加する
- **「研修管理」→「新メンバー追加」** で名前・ジョイン日を入力

### ステータスを更新する（1人30秒以内）
- 「メンバー一覧」シートの **C列（進捗ステータス）** を変更するだけ
- `未着手` → `進行中` → `完了` のドロップダウンから選択
- 最終更新日は**自動でセット**されます

### 進捗率を入力する（任意）
- **D列（進捗%）** に数値（0〜100）を入力

### ダッシュボードを確認する
- 「ダッシュボード」シートを見れば優先度順に一覧表示
- または **「研修管理」→「ダッシュボード更新」** で手動更新

---

## 色の見方

| 色 | 意味 | 推奨アクション |
|----|------|--------------|
| 🔴 赤 | 停滞（3日以上更新なし）または未着手が続いている | 状況確認（DM・電話） |
| 🟡 黄 | 軽微遅れ（1週間で30%未満） | 研修を進めるよう促す |
| 🟢 緑 | 問題なし | 対応不要 |

---

## 判定ロジック

| 条件 | 優先度 |
|------|--------|
| 最終更新日から3日以上経過 | 🔴 高 |
| ジョインから3日経過＆未着手 | 🔴 高 |
| ジョインから7日経過＆進捗30%未満 | 🟡 中 |
| それ以外 / 完了 | 🟢 低 |

---

## よくある質問

**Q: 停滞フラグや優先度を手動で変更できますか？**
A: F・G・H列はGASが自動計算します。手動変更しても次の自動実行時に上書きされます。

**Q: 毎日何時に自動実行されますか？**
A: 毎朝8時です。手動で今すぐ実行したい場合は「研修管理 → 停滞チェック実行」をクリックしてください。

**Q: メンバーを削除するには？**
A: 該当行を選択して右クリック → 「行を削除」で削除できます。
